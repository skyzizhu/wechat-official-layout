开发环境配置完整手册

> **导读**：以下代码均由纯文本自动识别语言，并套用对应的深色卡片样式。

## 一、前端组件示例

```typescript
function ArticleCard(props) {
  const { title, cover } = props;
  return {
    type: "card",
    title: title,
    cover: cover
  };
}
```

## 二、接口配置示例

```json
{
  "project": "wenpai",
  "version": "2.0.0",
  "features": ["auto-detect", "smart-table", "code-card"]
}
```

## 三、数据查询示例

```sql
SELECT theme_name, COUNT(*) as total FROM articles GROUP BY theme_name;
```

## 四、终端命令示例

```bash
npm install --save-dev wechat-formatter
npm run build -- --webpack
```